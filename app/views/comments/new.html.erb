

<%= form_with(model: [ @service, @comment=@service.comments.build ]) do |form| %>
    <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@comment.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @comment.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class = "form-group">
    <%= form.text_area :body %>
  </div>
  <p>
   <%= form.hidden_field :commenter, value: 'Moderator'%>
  </p>
  <p>
    <%= form.submit class: "btn btn-success" %>
  </p>
<% end %>

<%= link_to 'Back', pending_services_path , class: 'btn btn-secondary text-white' %>